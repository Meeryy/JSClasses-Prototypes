/*function Picture(painter, year,name,price) {
    this.painter = painter;
    this.year   = year;
    this.name = name;
    this.price = price;
}

Picture.prototype.getFullName = function () {
    return `${this.painter} - ${this.year}`;
}

function Magazine(painter, year, releaseNumber) {
    Picture.call(this, painter, year);
    this.releaseNumber = releaseNumber;
}

Magazine.prototype = Object.create(Picture.prototype);*/